OPENROUTER_API_KEY=sk-or-v1-...
PORT=3001
DATA_DIR=./data

# Clerk — required in production. Locally, omitting it uses a shared local user.
CLERK_SECRET_KEY=sk_test_...

# Optional beta gate — only set both if re-enabling Cloudflare Access on the API:
# CLOUDFLARE_ACCESS_AUD=<Access application AUD>
# CLOUDFLARE_ACCESS_TEAM_DOMAIN=https://sahilagentserver.cloudflareaccess.com
