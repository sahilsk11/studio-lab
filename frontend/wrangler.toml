name = "studiolab"
pages_build_output_dir = "dist"
compatibility_date = "2026-09-02"

# Fly origin for Pages Functions. The browser calls same-origin /api/*; Clerk and
# anonymous session headers are forwarded to Fly.
[vars]
API_ORIGIN = "https://studio-lab.fly.dev"
CANONICAL_HOST = "studiolab.ultron.sh"
